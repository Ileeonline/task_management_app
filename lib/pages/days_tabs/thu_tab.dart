import 'package:flutter/material.dart';

class ThuTab extends StatelessWidget {
  const ThuTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Center(
          child: Text('Thu'),
        ),
      ],
    );
  }
}
