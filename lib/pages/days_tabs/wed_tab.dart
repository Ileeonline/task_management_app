import 'package:flutter/material.dart';

class WedTab extends StatelessWidget {
  const WedTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Center(
          child: Text('Wed'),
        ),
      ],
    );
  }
}
