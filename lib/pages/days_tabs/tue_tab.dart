import 'package:flutter/material.dart';

class TueTab extends StatelessWidget {
  const TueTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Center(
          child: Text('Tue'),
        ),
      ],
    );
  }
}
